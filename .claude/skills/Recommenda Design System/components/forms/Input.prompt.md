Single-line text field — search, titles, names. Warm paper fill, soft ink border that turns blue on focus and red when `invalid`.

```jsx
<Input placeholder="Search recommendations" leadingIcon={<Search/>} />
<Input shape="pill" placeholder="Title" />
```
Use `shape="pill"` for search bars, `rounded` (default) for forms.
