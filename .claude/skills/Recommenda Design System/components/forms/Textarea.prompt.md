Multi-line note field for longer free text — e.g. why you're recommending something. Uses the large card radius to match surfaces.

```jsx
<Textarea rows={4} placeholder="Tell them why they'll love it…" />
```
