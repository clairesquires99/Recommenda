Generic surface container. `bordered` (default, soft border) for most groupings, `strong` for the hand-lined look, `inverse` for blue call-out panels.

```jsx
<Card>…</Card>
<Card variant="inverse" padding={24}>Recommenda Curation</Card>
```
