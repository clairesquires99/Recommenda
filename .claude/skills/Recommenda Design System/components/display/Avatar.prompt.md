Round friend avatar — a photo via `src`, or auto initials from `name`. Add `ring` to highlight the active user.

```jsx
<Avatar name="Sir Edward" size="md" />
<Avatar src="/u/mia.jpg" name="Mia" ring />
```
Sizes: `xs` (24) · `sm` (32) · `md` (44) · `lg` (64).
